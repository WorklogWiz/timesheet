{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "tui",
            "program": "${workspaceRoot}/target/debug/jira_worklog_tui",
            "cwd": "${workspaceRoot}",
            "sourceLanguages": [
                "rust"
            ],
            "preLaunchTask": "tui"
        }
    ]
}
